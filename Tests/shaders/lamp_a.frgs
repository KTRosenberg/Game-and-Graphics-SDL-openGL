#version 330 core
precision highp float;
in vec3 v_pos;
in vec3 v_nor;
in vec2 v_uv;
in vec3 frag_pos;

in vec3 light_position;

out vec4 color;

uniform float u_time;
uniform sampler2D tex0;
uniform sampler2D tex1;

void main(void)
{
    color = vec4(vec3(sqrt(1.0)), 1.0f);
}
